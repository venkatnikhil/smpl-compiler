main var a,b,c,d,e,z;{
    let a <- 10;
    let b <- 10 + 20 - 10;
    let z <- 5 + 10;
    let c <- b;
    let d <- c + c;
    let e <- b + c
}.


-- basic common subexpr test
-- common sub expr just above the instr to be created
